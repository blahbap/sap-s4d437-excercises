projection;
use draft;

define behavior for Z10_c_travel //alias <alias_name>
use etag
{
  use create;
  use update;
  use delete;
  use action set_to_cancelled;

  use action Edit;
  use action Activate;
  use action Discard;
  use action Resume;

}